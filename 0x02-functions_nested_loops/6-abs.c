#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @a: The parameter to be passed through the function
 * Return: the absolute value of the integer
 */
int _abs(int a)
{
	if (a > 0)
		return (a);
	else
		return (-a);
}
