#include "main.h"
#include <string.h>
/**
 * _strlen - function to check the length of a string
 * @s: parameter
 * Return: returns the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
